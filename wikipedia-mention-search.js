// Wikipedia mention fallback: broad discovery, strict validation, conservative fact extraction.
// v27: every automatically extracted fact carries the exact evidence sentence and source URL.
(function () {
  if (typeof fetchAutomaticHistory !== 'function') return;
  const previousFetchAutomaticHistory = fetchAutomaticHistory;
  const WP_API='https://he.wikipedia.org/w/api.php';
  const clean=v=>String(v||'').replace(/\[[0-9]+\]/g,'').replace(/\s+/g,' ').trim();
  const norm=v=>clean(v).replace(/["׳״'־–—-]/g,' ').replace(/^(רחוב|שדרות|שדרה|דרך|כביש|סמטה|סמטת)\s+/u,'').replace(/\s+/g,' ').trim().toLowerCase();
  function cityAliases(city){const n=norm(city);if(n==='ירושלים')return['ירושלים'];if(n==='תל אביב'||n==='תל אביב יפו')return['תל אביב','תל אביב יפו'];return n?[n]:[];}
  const cityMatches=(text,city)=>{const n=norm(text);return cityAliases(city).some(a=>n.includes(norm(a)));};
  const streetTokens=street=>[...new Set(norm(street).match(/[א-ת]+/gu)?.filter(t=>t.length>=2)||[])];
  const streetMatches=(text,street)=>{const n=norm(text),t=streetTokens(street);return t.length>0&&t.every(x=>n.includes(x));};
  const sentences=text=>clean(text).split(/(?<=[.!?])\s+(?=[א-ת"׳״])/u).map(s=>s.trim()).filter(s=>s.length>=12&&s.length<=1100);
  function contextSentences(text,street,city){const all=sentences(text),lead=cityMatches(text.slice(0,2600),city),idx=[];all.forEach((s,i)=>{if(streetMatches(s,street))idx.push(i);});const out=[];for(const i of idx)for(let j=Math.max(0,i-2);j<=Math.min(all.length-1,i+3);j++){const s=all[j];if((cityMatches(s,city)||lead)&&!out.includes(s))out.push(s);}return out.slice(0,16);}
  const ROAD_NOUN=/(?:הכביש|כביש|הרחוב|רחוב|הדרך|דרך|השדרה|שדרה|השדרות|שדרות|הציר|ציר)/u;
  const ROAD_EVENT=/(?:נחנך|נחנכה|נפתח|נפתחה|נסלל|נסללה|הוקם|הוקמה|הושלם|הושלמה|נבנה|נבנתה|נפרץ|נפרצה|החלה סלילתו|החלה סלילתה|החלו עבודות)/u;
  const NAMING_EVENT=/(?:נקרא|נקראה|נקבע|נקבעה|הוענק|ניתן|שונה שמו|שונה שמה|הוסב שמו|הוסב שמה)/u;
  const hasRoadSubject=(s,street)=>streetMatches(s,street)||ROAD_NOUN.test(s);
  function originEvidence(list,street){const sentence=list.find(s=>hasRoadSubject(s,street)&&/(?:נקרא|נקראת|קרוי|קרויה|נקראות|נקראים|מכונה)[^.!?]{0,220}(?:על שם|על־שם|על שמו|על שמה|לזכר|מנציח)/u.test(s));return sentence?{sentence}:null;}
  function yearEvidence(list,street,type){for(const sentence of list){if(!hasRoadSubject(sentence,street))continue;if(type==='founded'&&!ROAD_EVENT.test(sentence))continue;if(type==='named'&&(!NAMING_EVENT.test(sentence)||!/(?:השם|על שם|על־שם|על שמו|על שמה|שמו|שמה)/u.test(sentence)))continue;const m=clean(sentence).match(/(?:18\d{2}|19\d{2}|20\d{2})/u);if(m)return{value:m[0],sentence};}return null;}
  function plausibleFormerName(v,street){let c=clean(v).replace(/^["״']+|["״']+$/g,'').trim().replace(/^(?:בשם|כ|בכינוי)\s+/u,'').trim();if(!c||c.length<2||c.length>48||/[.!?:;,()\[\]{}]/u.test(c)||/\d{4}/u.test(c))return null;if(/\b(?:בשנת|לאחר|כאשר|שבו|שבה|אשר|עקב|בגלל|במהלך|נחנך|נסלל|נקרא|שונה|הוחלף|הוקם|הוביל|היה|הייתה|הוא|היא)\b/u.test(c))return null;const w=c.split(/\s+/);if(w.length>6||norm(c)===norm(street))return null;return /^(?:רחוב|שדרות|שדרה|דרך|כביש|סמטה|סמטת)\s+/u.test(c)||(w.length<=4&&!/^(?:של|את|אל|על|מן|עם|בו|בה|בין|ליד|לאורך)\b/u.test(c))?c:null;}
  function formerEvidence(list,street){const out=[];const patterns=[/(?:נקרא|נקראה)\s+(?:בעבר|קודם לכן|לפני כן|תחילה|בראשיתו|בראשיתה)\s+["״']?([^"״'.;,]{2,48})["״']?/u,/(?:שמו|שמה)\s+(?:הקודם|הקודמת)\s+(?:היה|הייתה)\s+["״']?([^"״'.;,]{2,48})["״']?/u,/(?:שונה|הוחלף|הוסב)\s+(?:שמו|שמה)\s+מ[־-]?["״']?([^"״'.;,]{2,48})["״']?\s+(?:ל|אל)/u];for(const sentence of list)for(const p of patterns){const m=sentence.match(p),name=m&&plausibleFormerName(m[1],street);if(name&&!out.some(x=>x.value===name))out.push({value:name,sentence});}return out.slice(0,4);}
  const useful=v=>Boolean(clean(v))&&!clean(v).includes('לא נמצא');
  function uniqSources(items){const seen=new Set();return(items||[]).filter(i=>{if(!i)return false;const k=typeof i==='string'?i:`${i.url||''}|${i.label||''}`;if(seen.has(k))return false;seen.add(k);return true;});}
  async function searchMentions(street,city){const base=norm(street),queries=[...new Set([`${base} ${city}`,`"${base}" ${city}`,base,`"רחוב ${base}" ${city}`,`"דרך ${base}" ${city}`,`"שדרות ${base}" ${city}`,`"כביש ${base}" ${city}`])],results=[];for(const q of queries){const p=new URLSearchParams({action:'query',list:'search',srsearch:q,srlimit:'10',srnamespace:'0',format:'json',origin:'*'});try{const d=await fetchJson(`${WP_API}?${p}`,`wp-mention-v27:${q}`);for(const x of d?.query?.search||[])if(!results.some(r=>r.title===x.title))results.push(x);}catch(e){console.warn('Wikipedia mention search failed:',e);}}return results.slice(0,24);}
  async function fetchArticle(title){const p=new URLSearchParams({action:'query',prop:'extracts|info',explaintext:'1',inprop:'url',titles:title,format:'json',origin:'*'}),d=await fetchJson(`${WP_API}?${p}`,`wp-mention-article-v27:${title}`),page=Object.values(d?.query?.pages||{})[0];return(!page||page.missing!==undefined||!page.extract)?null:{title:page.title,text:clean(page.extract),url:page.fullurl};}
  async function findEvidence(street,city){let best=null;for(const candidate of await searchMentions(street,city)){let article;try{article=await fetchArticle(candidate.title);}catch(_){continue;}if(!article||!cityMatches(article.text.slice(0,3200),city)||!streetMatches(article.text,street))continue;const ctx=contextSentences(article.text,street,city);if(!ctx.length)continue;const origin=originEvidence(ctx,street),founded=yearEvidence(ctx,street,'founded'),named=yearEvidence(ctx,street,'named'),former=formerEvidence(ctx,street);const score=(origin?5:0)+(founded?4:0)+(named?4:0)+(former.length?2:0)+Math.min(ctx.length,4);if(!best||score>best.score)best={article,ctx,origin,founded,named,former,score};}return best;}
  fetchAutomaticHistory=async function(street,city){let base=null;try{base=await previousFetchAutomaticHistory(street,city);}catch(e){console.warn('Base lookup failed:',e);}let ev=null;try{ev=await findEvidence(street,city);}catch(e){console.warn('Wikipedia evidence lookup failed:',e);}if(!ev)return base;const baseTrusted=base&&!base.wikipediaMentionFallback;const safeFormer=(base?.formerNames||[]).map(v=>plausibleFormerName(v,street)).filter(Boolean);const evidence={...(base?.evidence||{})};if(!useful(base?.origin)&&ev.origin)evidence.origin={sentence:ev.origin.sentence,label:`ויקיפדיה — ${ev.article.title}`,url:ev.article.url};if(!(baseTrusted&&base?.foundedYear)&&ev.founded)evidence.foundedYear={sentence:ev.founded.sentence,label:`ויקיפדיה — ${ev.article.title}`,url:ev.article.url};if(!(baseTrusted&&base?.namedYear)&&ev.named)evidence.namedYear={sentence:ev.named.sentence,label:`ויקיפדיה — ${ev.article.title}`,url:ev.article.url};if(ev.former.length)evidence.formerNames=ev.former.map(x=>({value:x.value,sentence:x.sentence,label:`ויקיפדיה — ${ev.article.title}`,url:ev.article.url}));return{...(base||{}),origin:useful(base?.origin)?base.origin:(ev.origin?`לפי ויקיפדיה: ${ev.origin.sentence}`:'לא נמצא במקורות שנבדקו'),foundedYear:baseTrusted&&base?.foundedYear?base.foundedYear:(ev.founded?.value||null),namedYear:baseTrusted&&base?.namedYear?base.namedYear:(ev.named?.value||null),formerNames:ev.former.length?ev.former.map(x=>x.value):safeFormer,description:useful(base?.description)?base.description:ev.ctx.slice(0,6).join(' ').slice(0,1250),sources:uniqSources([...(base?.sources||[]),{label:`ויקיפדיה — אזכור מאומת ב${ev.article.title}`,url:ev.article.url}]),evidence,automatic:true,wikipediaMentionFallback:true,strictRoadYearEvidence:true};};
})();
