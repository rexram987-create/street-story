// Wikipedia mention fallback: broad discovery, strict validation, conservative fact extraction.
// v26: a year is accepted only when the SAME sentence explicitly links it to the road/street itself.
(function () {
  if (typeof fetchAutomaticHistory !== 'function') return;
  const previousFetchAutomaticHistory = fetchAutomaticHistory;
  const WP_API = 'https://he.wikipedia.org/w/api.php';
  function clean(v){return String(v||'').replace(/\[[0-9]+\]/g,'').replace(/\s+/g,' ').trim();}
  function norm(v){return clean(v).replace(/["׳״'־–—-]/g,' ').replace(/^(רחוב|שדרות|שדרה|דרך|כביש|סמטה|סמטת)\s+/u,'').replace(/\s+/g,' ').trim().toLowerCase();}
  function cityAliases(city){const n=norm(city);if(n==='ירושלים')return['ירושלים'];if(n==='תל אביב'||n==='תל אביב יפו')return['תל אביב','תל אביב יפו'];return n?[n]:[];}
  function cityMatches(text,city){const n=norm(text);return cityAliases(city).some(a=>n.includes(norm(a)));}
  function streetTokens(street){return [...new Set(norm(street).match(/[א-ת]+/gu)?.filter(t=>t.length>=2)||[])];}
  function streetMatches(text,street){const n=norm(text),t=streetTokens(street);return t.length>0&&t.every(x=>n.includes(x));}
  function sentences(text){return clean(text).split(/(?<=[.!?])\s+(?=[א-ת"׳״])/u).map(s=>s.trim()).filter(s=>s.length>=12&&s.length<=1100);}
  function contextSentences(text,street,city){const all=sentences(text),cityInLead=cityMatches(text.slice(0,2600),city),idx=[];all.forEach((s,i)=>{if(streetMatches(s,street))idx.push(i);});const picked=[];for(const i of idx){for(let j=Math.max(0,i-2);j<=Math.min(all.length-1,i+3);j++){const s=all[j];if((cityMatches(s,city)||cityInLead)&&!picked.includes(s))picked.push(s);}}return picked.slice(0,16);}
  const ROAD_NOUN=/(?:הכביש|כביש|הרחוב|רחוב|הדרך|דרך|השדרה|שדרה|השדרות|שדרות|הציר|ציר)/u;
  const ROAD_EVENT=/(?:נחנך|נחנכה|נפתח|נפתחה|נסלל|נסללה|הוקם|הוקמה|הושלם|הושלמה|נבנה|נבנתה|נפרץ|נפרצה|החלה סלילתו|החלה סלילתה|החלו עבודות|נחנכה לתנועה)/u;
  const NAMING_EVENT=/(?:נקרא|נקראה|נקבע|נקבעה|הוענק|ניתן|שונה שמו|שונה שמה|הוסב שמו|הוסב שמה)/u;
  function hasRoadSubject(sentence,street){return streetMatches(sentence,street)||ROAD_NOUN.test(sentence);}
  function originFrom(list,street){return list.find(s=>hasRoadSubject(s,street)&&/(?:נקרא|נקראת|קרוי|קרויה|נקראות|נקראים|מכונה)[^.!?]{0,220}(?:על שם|על־שם|על שמו|על שמה|לזכר|מנציח)/u.test(s))||null;}
  function yearInSentence(sentence){const m=clean(sentence).match(/(?:18\d{2}|19\d{2}|20\d{2})/u);return m?.[0]||null;}
  function foundedFrom(list,street){for(const s of list){if(!hasRoadSubject(s,street)||!ROAD_EVENT.test(s))continue;const y=yearInSentence(s);if(y)return y;}return null;}
  function namedFrom(list,street){for(const s of list){if(!hasRoadSubject(s,street)||!NAMING_EVENT.test(s))continue;if(!/(?:השם|על שם|על־שם|על שמו|על שמה|שמו|שמה)/u.test(s))continue;const y=yearInSentence(s);if(y)return y;}return null;}
  function plausibleFormerName(v,street){let c=clean(v).replace(/^["״']+|["״']+$/g,'').trim().replace(/^(?:בשם|כ|בכינוי)\s+/u,'').trim();if(!c||c.length<2||c.length>48||/[.!?:;,()\[\]{}]/u.test(c)||/\d{4}/u.test(c))return null;if(/\b(?:בשנת|לאחר|כאשר|שבו|שבה|אשר|עקב|בגלל|במהלך|נחנך|נסלל|נקרא|שונה|הוחלף|הוקם|הוביל|היה|הייתה|הוא|היא)\b/u.test(c))return null;const words=c.split(/\s+/);if(words.length>6||norm(c)===norm(street))return null;const roadish=/^(?:רחוב|שדרות|שדרה|דרך|כביש|סמטה|סמטת)\s+/u.test(c),shortProper=words.length<=4&&!/^(?:של|את|אל|על|מן|עם|בו|בה|בין|ליד|לאורך)\b/u.test(c);return roadish||shortProper?c:null;}
  function formerFrom(text,street){const out=[],add=v=>{const c=plausibleFormerName(v,street);if(c&&!out.includes(c))out.push(c);};const patterns=[/(?:נקרא|נקראה)\s+(?:בעבר|קודם לכן|לפני כן|תחילה|בראשיתו|בראשיתה)\s+["״']?([^"״'.;,]{2,48})["״']?/gu,/(?:שמו|שמה)\s+(?:הקודם|הקודמת)\s+(?:היה|הייתה)\s+["״']?([^"״'.;,]{2,48})["״']?/gu,/(?:שונה|הוחלף|הוסב)\s+(?:שמו|שמה)\s+מ[־-]?["״']?([^"״'.;,]{2,48})["״']?\s+(?:ל|אל)/gu];for(const p of patterns)for(const m of clean(text).matchAll(p))add(m[1]);return out.slice(0,4);}
  function useful(v){const t=clean(v);return Boolean(t)&&!t.includes('לא נמצא');}
  function uniqSources(items){const seen=new Set();return(items||[]).filter(i=>{if(!i)return false;const k=typeof i==='string'?i:`${i.url||''}|${i.label||''}`;if(seen.has(k))return false;seen.add(k);return true;});}
  async function searchMentions(street,city){const base=norm(street),queries=[...new Set([`${base} ${city}`,`"${base}" ${city}`,base,`"רחוב ${base}" ${city}`,`"דרך ${base}" ${city}`,`"שדרות ${base}" ${city}`,`"כביש ${base}" ${city}`])],results=[];for(const query of queries){const p=new URLSearchParams({action:'query',list:'search',srsearch:query,srlimit:'10',srnamespace:'0',format:'json',origin:'*'});try{const d=await fetchJson(`${WP_API}?${p}`,`wp-mention-v26:${query}`);for(const item of d?.query?.search||[])if(!results.some(r=>r.title===item.title))results.push(item);}catch(e){console.warn('Wikipedia mention search failed:',e);}}return results.slice(0,24);}
  async function fetchArticle(title){const p=new URLSearchParams({action:'query',prop:'extracts|info',explaintext:'1',inprop:'url',titles:title,format:'json',origin:'*'});const d=await fetchJson(`${WP_API}?${p}`,`wp-mention-article-v26:${title}`),page=Object.values(d?.query?.pages||{})[0];return(!page||page.missing!==undefined||!page.extract)?null:{title:page.title,text:clean(page.extract),url:page.fullurl};}
  async function findMentionEvidence(street,city){const candidates=await searchMentions(street,city);let best=null;for(const candidate of candidates){let article;try{article=await fetchArticle(candidate.title);}catch(_){continue;}if(!article||!cityMatches(article.text.slice(0,3200),city)||!streetMatches(article.text,street))continue;const ctx=contextSentences(article.text,street,city);if(!ctx.length)continue;const ev={article,ctx,originSentence:originFrom(ctx,street),foundedYear:foundedFrom(ctx,street),namedYear:namedFrom(ctx,street),formerNames:formerFrom(ctx.join(' '),street)};const score=(ev.originSentence?5:0)+(ev.foundedYear?4:0)+(ev.namedYear?4:0)+(ev.formerNames.length?2:0)+Math.min(ctx.length,4);if(!best||score>best.score)best={...ev,score};}return best;}
  fetchAutomaticHistory=async function(street,city){let base=null;try{base=await previousFetchAutomaticHistory(street,city);}catch(e){console.warn('Base lookup failed:',e);}let ev=null;try{ev=await findMentionEvidence(street,city);}catch(e){console.warn('Wikipedia mention fallback failed:',e);}if(!ev)return base;const safeFormer=(base?.formerNames||[]).map(v=>plausibleFormerName(v,street)).filter(Boolean);const baseYearTrusted=base&&!base.wikipediaMentionFallback;return{...(base||{}),origin:useful(base?.origin)?base.origin:(ev.originSentence?`לפי ויקיפדיה: ${ev.originSentence}`:'לא נמצא במקורות שנבדקו'),foundedYear:baseYearTrusted&&base?.foundedYear?base.foundedYear:(ev.foundedYear||null),namedYear:baseYearTrusted&&base?.namedYear?base.namedYear:(ev.namedYear||null),formerNames:ev.formerNames.length?ev.formerNames:safeFormer,description:useful(base?.description)?base.description:ev.ctx.slice(0,6).join(' ').slice(0,1250),sources:uniqSources([...(base?.sources||[]),{label:`ויקיפדיה — אזכור מאומת ב${ev.article.title}`,url:ev.article.url}]),automatic:true,wikipediaMentionFallback:true,strictRoadYearEvidence:true};};
})();
